var 宣告變數，此為區域變數(scope)
不加var為全域變數(global variable)
變數大小寫有別，不能用數字開頭，有保留字(有些字已經被設定過意義)


函數 function

物件 大括號{}寫入一至多種屬性的內容集合

jQuery是用JS語言包裝起來，用jQuery能少寫JS
使用需載入，有兩種方式()


監聽節點
做什麼動作 
放入什麼東西
效果

監聽節點
做什麼動作 
取值 (var)
(var) 放入節點
效果





